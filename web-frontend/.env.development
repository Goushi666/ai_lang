# 开发推荐留空：请求走当前页面源（5173），由 Vite 代理到后端（见 vite.config.js），无跨域问题。
# 若填 http://localhost:8000，则为直连后端，需后端 CORS 正确（且不要用 allow_origins=* + credentials=true）。
VITE_API_BASE_URL=
VITE_WS_URL=

# 后端地址（仅用于 Vite 代理，改端口时改这里）
VITE_DEV_PROXY_TARGET=http://127.0.0.1:8000

